# 更新历史

### 0.9.4.1

- 修复很多直播间实际不生效的问题：这些房间的播放器在页面内嵌的 live.bilibili.com/blanc iframe 里，而插件只注入顶层页面，整个直播模块在空转。现在扩展在直播域对 iframe 也注入（`all_frames`），油猴脚本去掉 `@noframes` 并只在直播域的 iframe 里继续运行，其他站内 iframe 直接退出。
- 油猴菜单的“线程撕裂者设置”只在顶层页面注册一次。

### 0.9.4.0

- 新增直播加速模块（live.bilibili.com）。海外看直播卡顿的主要来源是分到的直播节点质量参差且经常波动（实测同组节点首字节 73 毫秒到 4.5 秒不等），以及网页播放器的 P2P 在海外观众稀少时纯属拖累。模块做三件事：
  - 屏蔽直播 P2P（PCDNLoader、BPP2PSDK、SeederSDK），mcdn/PCDN 地址和 smtcdns 中转地址改写回官方节点；
  - 直播分片改为节点池竞速下载：官方给的节点加上实测签名互认的 fmp4 同组节点（含大陆节点），按首字节速度排序，慢节点 400 毫秒后自动开备份副本，两次空响应的节点整场停用；
  - 提前缓存 playlist 里已宣告的分片，播放器来要的时候直接命中（真实直播间实测：分片就绪延迟中位数从 600-1200 毫秒降到 0，均值降到一半以下；原分配节点中途故障时自动切换，避免了基线里 5.8 秒的卡顿）。
- 设置面板新增“直播加速”开关，默认开启；总开关关闭时直播模块也随之关闭。
- 直播只处理 fMP4 HLS 流（网页播放器默认）；FLV 是连续流，没有可并行的分片，不做处理。
- 新增 `dev/live-core-test.js`（单元测试）、`dev/live-hook-test`（浏览器回归）和 `dev/live-smoke-test.js`（真实直播间冒烟，需要网络）。

### 0.9.3.0

- 中断的小段从断点续传：重试和备份副本只下载还缺的字节，拼上已经收到并校验过位置的部分，不再整块重新下载。一条线路中途停传时恢复更快，重复下载的流量也大幅减少（模拟测试里浪费流量从约 13% 降到 3% 以下）。
- 子块按各节点的实测速度分配：快节点多领，慢于最快节点十二分之一的节点不再作为首选（探索位仍会不时试探它，恢复了就重新参与）。备份副本改发给已知最快的节点，不再按固定顺序轮到最慢的那个。
- 子块大小跟随实测连接速度自动调整（64 KiB 到 1 MiB）：高延迟线路上高线程数不再发出大量 64 KiB 小请求，同时每段仍保证拆给所有可用节点。
- 浏览器媒体缓冲区满（QuotaExceededError）时不再把视频交回 B 站：释放已播放的数据后重试这次写入，并降低这个视频的预缓冲上限。4K 高码率视频以前可能在缓冲将满时突然回退原生连接。
- 下载地址快过期（地址里的 deadline）前两分钟自动向 B 站换新地址，播放和进度不受影响。以前长时间暂停后恢复播放会先集中收到 403、卡一下才恢复。
- 新增 `dev/download-benchmark.js`：在模拟的多节点网络上对比两个版本的下载内核（吞吐、浪费流量、最慢分段）。

### 0.9.2.3

- 修复连续拖两次进度条（第二次拖的时候第一次还在加载）之后视频停住不播、之后每次拖动都保持暂停的问题。缓冲不足时插件自己暂停补缓冲，这时拖进度条或换清晰度也会这样。现在会记住你原本是在播放的。
- 下载出错掉回 B 站原来的连接、几秒后自动重新接管时，原来在播就接着播。以前重新接管后常常停在暂停状态，要自己再点一下播放。
- 诊断信息（控制台运行 `copy(__biliThreadRipperDebug.report())`）里多了一段页面级的记录：什么时候接管、什么时候播放失败、交回 B 站、B 站自己换了视频源，以及当时的播放位置。遇到“黑屏后进度回退”这类问题时，出现后马上复制一份贴到 issue 里就能定位。

### 0.9.2.2

- 支持“稍后再看”和收藏夹页面的播放加速（#19，感谢 silverair）。以前只认 `/video/BV…` 这种地址，在这两个页面上插件不生效。

### 0.9.2.1

- 修复开了“单集循环”后，视频播完回到开头就停住、不再自动播下一轮的问题（#17）。播完后点“重播”也是同一条路径。这个问题从 0.9.1.x 就有，不是 0.9.2.0 新出现的。
- 设置面板新增“全接管 / 兼容模式”开关，放在以前兼容模式的位置，默认全接管。兼容模式下还是 B 站自己的播放器在放视频，插件只帮它多线程下载，换清晰度、换编码都交给 B 站；全接管则由插件自己放视频，效果最好。Safari 上全接管切清晰度会失败，Safari 用户建议用兼容模式。兼容模式的做法来自 Awes0meE 的 #16，这里改成所有浏览器都能选，只有选了才会接管请求。
- 下载器的几处修补（取自 #16）：排队中的小段在取消时立刻放弃，不再占着线程；读取中的连接在取消时主动断开；校验失败的响应也会关掉。

### 0.9.2.0

- 视频编码跟随 B 站播放器的“播放策略”（⚙ → 更多播放设置）：选了 AV1、HEVC 或 AVC 就优先用它，这个清晰度没有时再按默认顺序；选“默认”仍是 AV1 > HEVC > AVC。播放中改了会马上换成新编码，接着当前进度播放。以前接管后总是用 AV1，老电脑容易卡、发热。
- 右键“视频统计信息”的 Video Speed、Audio Speed 改成和 B 站原生一样：显示下载时的实际速度，两段下载之间保留上一次的数值，不再跳 0 或忽高忽低。Player Type 显示“BTR Native”，Resolution 显示插件实际在播的分辨率。
- CDN 增加“自定义”，在大陆、海外后面：可以勾选已知的服务器，也可以手动添加服务器地址，插件只用你选的这些。只收 B 站的视频服务器（bilivideo.com、akamaized.net 等），视频下载地址不会发给别的网站。一个都没选时按大陆 CDN 下载。
- 播放中换 CDN（包括改自定义服务器）不再让视频从头开始，新的选择用在接下来的下载上。
- 扩展和油猴用同一个设置面板：在 B 站页面上点扩展图标打开（油猴版在油猴菜单里点“线程撕裂者设置”），播放器 ⚙ 里点“自定义”也会打开。不再用 Chrome 侧边栏，扩展权限只剩保存设置一项。
- 删除兼容模式 A、B，只保留标准模式，旧的兼容模式设置会自动清掉。
- 清理旧版残留：0.8.x 的 ArtPlayer 播放器、弹幕和字幕代码、首屏遮罩，以及只给它们用的后台请求和测试。

### 0.9.1.5

- 修复关掉 B 站播放器的“自动开播”后，插件接管完视频还是会自己播放的问题（#13）。在页面里换视频或重新接管时，原来在播就接着播，原来暂停就保持暂停。
- 只拿到 akamaized.net 下载地址的账号也能用多个节点下载，大陆 CDN 模式不再直接接管失败；某个节点只是拒绝其中一个地址时，只停用这一个组合，不会把整个节点停掉（#9，感谢 KaiC5504）。
- 换节点时不再带上原地址的端口。有些 PCDN 地址带 `:4483`，以前换出来的地址会全部连不上（#9）。
- 一小段下载失败后会再试两轮；掉回 B 站原来的连接后会自动重新接管，最多 3 次（#9）。
- 打开视频时预先连上 CDN 节点；B 站自己的播放地址先回来时直接用它接管；分段改成下完一段就补一段（#10，感谢 KaiC5504）。
- 修复开播前要求的缓冲越等越多、开播被拖慢的问题；拖到没缓冲的位置时不再重新下载视频头和索引；“视频统计信息”的 Segments 一行会显示跳转后多久恢复播放；在控制台运行 `copy(__biliThreadRipperDebug.report())` 可以复制诊断信息贴到 issue 里（#12，感谢 KaiC5504）。

### 0.9.1.4

- 修复在 B 站清晰度菜单里换了清晰度，插件却还在播原来那个清晰度的问题（#5）。切换时接着当前进度播放，切换途中拖进度条也有效，重复选同一个清晰度不会重新加载。选“自动”时播放这个视频默认的清晰度。
- 播放器右键“视频统计信息”改为显示插件实际的数据：下载节点、速度、分段、码率和丢帧，Player Type 一行写明由线程撕裂者接管（#6）。
- 不再隐藏 B 站播放器自己的提示，比如切换清晰度的提示。B 站原生内核的报错在接管期间照样隐藏，但会记到 Debug 日志里（#6）。
- 修复油猴版设置页的“关闭”按钮有时点不到、点击落到下面网页上的问题（#8）。设置页现在总在最上层；窗口很矮时，“关闭”按钮也固定显示在底部。
- 更新历史移到 updates.md，最新的在最上面（#7）。

### 0.9.1.3

- 修复视频播到结尾时一直显示缓冲、不能正常结束的问题（HEVC 编码最常见，进度条最后会留一小段灰色）。

### 0.9.1.2

- 默认改为大陆 CDN、8 线程。从旧版升级时，CDN、线程数和“显示错误”会统一改成新的默认值一次，其他设置保留。
- 红色错误提示默认不再显示，需要排查时在侧边栏打开“显示错误”，打开后和以前一样。
- 同一个视频里，某个 CDN 节点两次一点数据都没返回（错误提示显示“已收到 0 KiB”）就停用它，这个视频接下来不再使用，换视频后恢复。所有节点都被停用时仍会继续尝试，避免视频完全放不了。
- 侧边栏在播放器里改了线程数或 CDN 后会同步显示。

### 0.9.1.1
- 增加debug消息功能
- 增加错误提示功能，方便定位错误

### 0.9.1.0

- 修复切换视频后插件不再接管的问题：播放接口统一使用 `api.bilibili.com`，避免 `www` 域名返回 HTTP 404。
- 修复新路由首个有效 `playurl` 响应因 CID 尚未初始化而被错误拒绝的问题。
- 隔离快速 SPA 跳转、合集切换过程中旧页面和旧请求的迟到响应。
- 新增左下角接管错误提示、错误日志和“重新接管”入口。
- 新增标准模式、兼容模式 A、兼容模式 B 三种选择。
- 补充 API 域名、快速切换、接管错误和兼容模式回归测试。

### 0.9.0.4

这一版重点修复合集、多 P 视频频繁切换后卡死，必须刷新页面才能恢复的问题。

- 修复合集或多 P 在短时间连续点击时，最后一次选择被旧切换任务吞掉的问题。
- 同一 BV 的播放清单现在同时校验 CID，旧 P 的迟到响应不能再污染当前视频。
- 每一代播放器都有独立生命周期标识，旧视频的错误回退和媒体源回调不能再关闭新播放器。
- 合集切换只拆除插件自己的 MediaSource，不再短暂重新加载上一个视频的 blob 地址。
- `sourceopen`、`updateend` 等等待现在会随旧任务一起取消，避免频繁切换后积累悬挂任务。
- 新增快速 A→B→C 和同 BV 多 P 响应乱序回归测试。

### 0.9.0.3

- 加速内核会承认预缓冲期间已经播放出去的进度。
- 同一个视频接管时只能保持当前位置或向前。
- 最终激活阶段禁止向后 seek。
- 跨 BVID/合集切换仍然可以明确从 0 秒开始。
- 普通历史进度仍然精确保留。
- 增加“原生播放器提前播放”的连续性回归测试。
- 实测预缓冲期间播放到约 5.75 秒，倒退次数为 0。
- 同时保留 `0.9.0.2` 后期的合集、6004 和短视频结尾修复。

### 0.9.0.2

- 原生播放器已经开始播放后，插件接管时不再无条件回到零秒。
- 尝试保存精确 `currentTime`。
- 减少热门视频播放一瞬间后卡帧。
- 改善初始 SourceBuffer 建立和首段追加。
- 修复合集切换时读取已经被 B 站销毁的 SourceBuffer。
- 修复因此产生的 `InvalidStateError` 和错误码 6004。
- 点击合集条目之前先把媒体源控制权还给 B 站。
- 终止旧视频线程、定时器和异步请求。
- 地址栏没有及时变化时，根据右侧合集活动项识别新 BVID。
- 修复把上一个视频的播放位置带到下一个视频。
- 跨 BVID 切换强制从 0 秒开始，同时保留播放/暂停意图。
- 期间还更新了 README、演示图片和 B 站官方 CDN 架构说明，但这些不属于播放器功能变化。
- 仓库里的 `0.9.0.2_` 只是一次 README 标签，不是新的插件版本。

### 0.9.0.1

- 放弃效果较差的 XHR 层植入方案。
- 改为“B 站原生界面 + 插件 Progressive MSE 内核”。
- 把 `0.8.x` 的 SIDX、Range 拆分、并发下载和 CDN 解析重新接到原生 `<video>` 下。
- 视频、音频分别建立 SourceBuffer。
- 保留真正的多 Range 并发，不是简单预取。
- 跳转进度条时按目标位置重新建立连续缓存。
- 缓冲超过三秒时增加解释性提示。
- 设置入口最终恢复到 B 站齿轮菜单的二级菜单，避免一级菜单布局冲突。

### 0.9.0

- 不再用 ArtPlayer 替换整个播放器。
- 保留 B 站原生播放器界面。
- 清晰度、弹幕、字幕、快捷键、小窗、全屏和自动连播重新交给 B 站负责。
- 插件只负责底层媒体下载和加速。
- CDN 和线程设置植入 B 站播放器齿轮菜单。
- 增加首次使用引导。
- 默认大陆 CDN、32 线程。
- 解决 `0.8.x` 自建播放器长期出现的字幕和功能兼容问题。

此版本问题很严重，不要使用

### 0.8.9.2

- 修复字幕经常 `Failed to fetch`。
- 字幕下载增加超时、重试和大小校验。
- 切换视频时取消上一条字幕请求。
- 对字幕响应严格校验 BVID、AID 和 CID。
- 修复新视频显示上一条视频字幕的严重问题。
- 修复字幕内容无法正确换行。
- 点击 CC 按钮可以直接关闭字幕。
- 关闭字幕时显示带斜杠的 CC 状态。
- 调整字幕图标对齐。
- 补充音量调节。
- 加强合集、分 P 和站内跳转时的资源隔离。

这一版插件拥有整个播放器，所以调度自由度最高，有时确实会比早期 `0.9.x` 更流畅

### 0.8.9.1

- 修复已知问题

### 0.8.9

- 增加 B 站字幕支持。
- 识别普通字幕、中文、简体中文和“中文（自动生成）”等字幕类型。
- 增加字幕切换和关闭。
- 修复小窗模式底栏元素挤压、错位。
- 隐藏使用率较低的网页全屏入口。
- 调整字幕菜单、CC 图标和菜单提示，避免提示文字挡住选项。

### 0.8.8.1

- 播放器参数写入 Chrome Storage。
- CDN 模式、线程数、弹幕字号、透明度等设置切换视频后不再丢失。
- 页面刷新和重新打开浏览器后继续使用上次设置。
- 修复播放器重新创建时把设置恢复为默认值的问题。

### 0.8.8

- 在弹幕设置中增加文字大小调节。
- 调整弹幕显示比例、透明度和位置。
- 改善小窗状态下弹幕和控制栏的布局。

### 0.8.7

- 修复从合集、推荐或其他视频跳转后，页面已经变了但播放器仍播放旧视频。
- 开始严格区分 BVID、CID 和分 P。
- 路由改变时终止旧视频的下载任务。
- 新视频必须拿到匹配的播放清单后才能接管。
- 增加站内 SPA 导航回归测试。

### 0.8.6

当时有过这一阶段的安装包迭代，但仓库没有正式 `0.8.6` 标签。内容主要是 `0.8.5` 后续的小修，最终合并进了 `0.8.7`。

### 0.8.5

- 解决视频先播放一瞬间，然后再次转圈的问题。
- 调整首次缓冲量和连续缓存策略。
- 大幅跳转进度条时重新建立目标位置缓存。
- 修复播放器接管过程中重复加载媒体源。
- 编码优先级调整为 `AV1 > HEVC/H.265 > AVC/H.264`。
- 加载界面改为 Material Design 圆形动画。
- 正常加载时不再显示大段文字，只有错误才提示。

### 0.8.4

这一版重点解决“先看见原播放器，然后过几秒才被替换”的问题。

- 加入 Early Mask，播放器接管完成前隐藏原版画面。
- 不再等整个媒体块下载完才开始追加。
- 视频和音频分段可以边下载边写入 MSE。
- 改善第一次打开视频时的卡顿。
- 修复部分跳转进度条后的 `updateend`/SourceBuffer 异常。
- 补充视频播放结束时的处理。
- 减少原播放器和新播放器同时工作的时间。

### 0.8.2～0.8.3

- 修补清晰度切换。
- 修复默认静音。
- 改善自动播放。
- 把 CDN 模式和线程数放进播放器设置。
- 默认改为大陆 CDN。
- 优化第一次打开视频时的播放清单获取。
- 补齐弹幕控制栏和播放器基本功能。

### 0.8.1

- 使用自建播放器替换 B 站播放器。
- 接管 DASH 视频和音频。
- 解析 SIDX 分段索引。
- 将媒体 Range 继续拆成多个小块，像 IDM 一样并发下载。
- 支持大陆 CDN、海外 CDN。
- 支持多 CDN 线路切换和失败重试。
- 初步实现清晰度、自动播放、弹幕读取和弹幕发送。
- 添加播放器设置、侧边栏速度显示和线程统计。
